{
  "name": "multi-model-api-platform",
  "private": true,
  "packageManager": "pnpm@10.34.2",
  "scripts": {
    "dev": "pnpm --parallel --filter './apps/*' dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
